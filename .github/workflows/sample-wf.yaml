name: GitHub Actions デモ
run-name: ${{ github.actor }}がGitHub Actionsを[>
on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo " 　ブランチ名は ${{ github.re>
  - name: リポジトリのコードをチェックアウトする
        uses: actions/checkout@v4
      - run: echo " リポジトリ ${{ github.reposi>
  - name: リポジトリ内のファイルを一覧表示
        run: |
          ls ${{ github.workspace }}
      - run: echo " このジョブステータスは ${{ j>











































